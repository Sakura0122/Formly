# 表单管理

## 当前状态

### 已完成
- 已完成表单管理页主流程，覆盖目录树、本地搜索、分组/表单创建、重命名、移动、删除、顶部详情展示与预览空态。
- 已完成首页路由调整，请求层复用，以及 `form-group / form-definition` API 模块封装。
- 已完成左侧目录树切到 `el-tree`、滚动容器切到 `el-scrollbar`，并收口创建/重命名/移动/删除类弹窗实现。
- 已完成管理页右侧发布态预览首版接入，并保持“无发布版本时展示空状态”的交互约束。
- 已通过 `npm run type-check` 与 `npm run build` 验证前端构建链路。
- 已完成右侧真实预览：仅在存在已发布版本时展示只读表单，否则固定显示空状态。
- 已完成管理页读取接口收口：右侧预览只消费发布态单个 `schema`，不再接收草稿相关字段。
- 已完成管理页版本展示收口：顶部版本改为展示发布版本号文案 `V1 / V2`，不再直接显示版本 ID。

### 未完成
- 复制表单与数据管理仍未接入完整业务能力。

### 当前问题
- 右侧预览区的上下留白仍落在非滚动层，滚动时会残留固定空白，影响画布沉浸感。

## 实施计划
- 继续微调右侧预览滚动层级：保留画布留白，但把上下留白移动到滚动内容内部，让空白随滚动一起移动。
- 在版本语义收口后，再继续接入复制表单与数据管理页。
- 若要进一步贴近设计稿，可在联调阶段补目录懒加载、更多节点操作与版本标签展示。
- 继续优化管理页结构：后续可再把目录树筛选、选中恢复等页面级逻辑抽成 composable，进一步压缩父组件体积。

## 执行记录
- 2026-04-14：完成管理页主视图、请求封装、目录树/弹窗主流程，以及首页路由切换；并通过 `npm run type-check` 与 `npm run build`。
- 2026-04-14：继续收口管理页结构，统一修正相关 ID 为字符串类型，并完成左侧目录树切换为 `el-tree`、滚动容器切换为 `el-scrollbar`。
- 2026-04-14：将删除流程改为勾选式二次确认弹窗，并将创建/重命名/移动类弹窗统一收口为 `el-form + rules + validate`。
- 2026-04-15：将管理页右侧详情查询切到 `/form-definitions/editor/{id}`，与编辑器页复用同一份表单编辑数据；再次通过 `npm run type-check` 与 `npm run build`。
- 2026-04-15：确认版本标签不再单独落库，后续若管理页需要展示 `V31` 这类文案，统一基于 `versionNo` 动态拼接即可。
- 2026-04-15：重写管理页计划，明确右侧预览只展示已发布版本；无发布版本时保持空状态，不再回退展示草稿内容。
- 2026-04-15：完成右侧发布态只读预览接入，并与编辑器独立草稿预览复用同一套渲染层；已通过 `npm run type-check` 与 `npm run build`。
- 2026-04-15：重写管理页计划，改为使用独立发布态读取接口，只返回单个 `schema`，并彻底移除对编辑器双 schema 返回的依赖。
- 2026-04-15：完成管理页独立发布态读取接口接入，右侧预览统一直接消费 `schema` 表格数据；已通过 `npm run type-check` 与 `npm run build`。
- 2026-04-15：继续微调管理页预览滚动体验，准备把上下留白从外层容器移入滚动内容，避免滚动时顶部/底部残留固定空白。
- 2026-04-15：补齐管理页发布态详情 `publishedVersionNo` 并完成顶部版本展示收口，改为显示 `V1 / V2`；已通过 `npm run type-check`。
