# 后端数据库设计

## 当前状态

### 已完成
- 已明确数据库阶段目标为支撑管理页、编辑器持久化与后续数据管理页，并确认使用 MySQL。
- 已确定采用“完整历史版本 + 整份 schema JSON 落库 + 提交记录绑定发布版本”的持久化方向。
- 已完成 `form_group / form_definition / form_version / form_submission` 四张核心表的初始化脚本，并收口为“`form_definition.draft_schema_json` 挂草稿、`form_version` 仅承载发布历史”的新模型。
- 已删除冗余 `version_label / current_version_id / is_published`，当前数据库仅保留 `published_version_id` 与 `version_no` 作为发布态主线。

### 未完成
- 更完整的提交记录查询、版本审计信息与后续数据管理能力仍待继续落地。

### 当前问题
- 当前表结构已足够支撑首版链路；若后续要增强发布审计，再评估补充发布人、发布说明等元数据。

## 实施计划
- 继续沿用“草稿挂定义、发布落版本”的结构补齐后续版本/提交相关接口；若后续需要更强审计，再单独增加元数据字段。

## 执行记录
- 2026-04-09：完成数据库范围收敛与 `backend/sql/init.sql` 首版脚本，覆盖分组、表单、版本、提交记录四张核心表及必要索引。
- 2026-04-15：确认保存/保存并发布首版闭环继续沿用现有表结构，`schema_json` 当前统一按裸 `table` 数据落库。
- 2026-04-15：移除冗余 `version_label`，版本展示统一改为基于 `version_no` 动态拼接。
- 2026-04-15：完成草稿/发布版本解耦，`form_definition` 新增 `draft_schema_json`，`form_version` 改为只承载发布历史，并同步删除 `current_version_id / is_published`。
