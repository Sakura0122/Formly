# 表单预览

## 当前状态
- 已完成：预览页与编辑器内预览的文字自适应高度改造；`/form-preview` 交互态已改为整格承载输入，补上可编辑单元格浅底色与点击高亮。
- 已完成：预览页交互态的行高估算、字段高度与内边距已回收到设计态基线，单元格尺寸不再额外放大。
- 已完成：移除预览容器与选项类字段残留的 `py-0.5` 垂直留白，进一步压缩高度误差。
- 已完成：单选默认右外边距、下拉箭头距离过大、上传按钮未居中的问题已在预览层修正。
- 已完成：上传高度异常已通过清理空 `el-upload-list` 外边距修正；下拉已补上整高容器参与垂直居中。
- 已完成：下拉 `el-select__wrapper` 已恢复整格宽度，避免内容宽度收缩导致显示异常。
- 已完成：`form-preview-field.vue` 样式块已切换为 `scss` 嵌套写法，便于后续继续维护预览皮肤。
- 未完成：待实际页面联调确认不同字段类型在预览页中的视觉细节与尺寸是否都符合设计态。
- 当前问题：下拉、日期、上传等非纯文本字段仍保留各自组件特征，若后续要继续向 Excel 视觉收敛，还需要单独细化。

## 实施计划
- 回收 `/form-preview` 交互态的行高估算、字段高度和内边距，尽量与设计态共用同一套尺寸基线。
- 去掉预览层残留的 `py-0.5` 垂直 padding，继续把单元格高度压回设计态。
- 修正单选/多选默认外边距与上传居中问题，并收紧下拉文字和箭头间距。
- 去掉空上传列表的默认外边距，并让下拉根节点按整格高度垂直居中。
- 让下拉 `wrapper` 重新铺满整格宽度，同时保留较紧凑的内部间距。
- 将预览样式块改为 `scss` 嵌套结构，保持样式逻辑不变但提升可维护性。
- 联调 `/form-preview` 页面，确认文本、数字、下拉、日期在整格输入态下的对齐与交互是否一致。
- 根据联调结果继续微调非文本字段的视觉细节，必要时再区分字段类型做更细颗粒处理。
- 将 `editor-canvas.vue` 和 `form-preview-canvas.vue` 中的 JS 行高估算逻辑全部删除，
`gridTemplateRows` 统一使用 `minmax(原始行高, auto)`，由 CSS Grid 自动撑开内容。

## 执行记录
- 2026-04-15：完成预览相关 4 个文件的文字自适应高度改造。
- 2026-04-15：追加 `/form-preview` 交互样式优化计划，并完成预览单元格浅底色、点击高亮、输入控件去边框感改造。
- 2026-04-15：补充预览尺寸回调计划，并完成交互态行高估算、内边距和控件留白回收，使预览尺寸贴近设计态。
- 2026-04-15：追加高度微调项，并移除预览层残留的 `py-0.5` 垂直留白。
- 2026-04-15：补充组件对齐修正计划，并完成单选外边距、下拉箭头间距和上传居中问题修正。
- 2026-04-15：补充上传/下拉二次修正计划，并完成空上传列表高度和下拉垂直居中问题修正。
- 2026-04-15：追加下拉三次修正计划，并完成 `wrapper` 整格宽度恢复与紧凑间距保留。
- 2026-04-15：补充样式风格修正计划，并完成预览样式块 scss 嵌套化整理。
- 2026-04-15：删除两个 canvas 组件中重复的行高估算函数，改为 CSS Grid 原生自适应
