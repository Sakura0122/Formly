# 表单预览

## 当前状态

### 已完成
- 已完成预览页与编辑器内预览的文字自适应高度改造，`/form-preview` 交互态已改为整格承载输入，并补齐可编辑单元格浅底色与点击高亮。
- 已完成尺寸基线收口：删除 JS 行高估算，统一交给 CSS Grid 自适应；行高、字段高度、内边距与残留 `py-0.5` 留白均已回收到设计态附近。
- 已完成组件细节修正：单选外边距、下拉箭头间距、上传居中、空上传列表外边距、下拉整高垂直居中与 `wrapper` 整格宽度均已修正。
- 已完成样式整理：`form-preview-field.vue` 样式块已切换为 `scss` 嵌套写法；单选/多选文案改为多行展示，窄列下不再省略。

### 未完成
- 待实际页面联调确认不同字段类型在预览页中的视觉细节与尺寸是否都符合设计态。

### 当前问题
- 下拉、日期、上传等非纯文本字段仍保留各自组件特征；若后续要继续向 Excel 视觉收敛，还需要按字段类型继续细化。

## 实施计划
- 联调 `/form-preview` 页面，确认文本、数字、下拉、日期在整格输入态下的对齐与交互是否一致。
- 根据联调结果继续微调非文本字段的视觉细节，必要时再区分字段类型做更细颗粒处理。

## 执行记录
- 2026-04-15：完成预览文字自适应高度、交互态整格输入、尺寸基线回收、残留留白清理、单选/下拉/上传细节修正、样式块 `scss` 嵌套化，以及 `editor-canvas.vue`、`form-preview-canvas.vue` 的 JS 行高估算删除并切到 CSS Grid 原生自适应。
- 2026-04-16：补充单选/多选文案换行计划，并完成编辑器预览与表单预览中的省略号移除，交互态选项项高同步改为随文案自适应；已通过 `npm run type-check`。
