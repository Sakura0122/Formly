# 表单预览 - 文字自适应高度

## 当前状态
- 已完成：三个文件的文字自适应高度改造

## 实施计划

### 改动点
1. `form-preview-canvas.vue`：gridTemplateRows 改为 `minmax(min, auto)`
2. `form-preview-field.vue`：text/textbox/number 只读态去 truncate、h-6 → min-h-6 + break-words
3. `editor-field-preview.vue`：同上
4. `editor-canvas.vue`：gridTemplateRows 数据行改为 `minmax(min, auto)`

## 执行记录
- 2026-04-15：完成全部 4 个文件改动
