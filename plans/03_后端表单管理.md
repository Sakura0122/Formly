# 后端表单管理

## 当前状态

### 已完成
- 已完成表单分组与表单定义两个模块的后端 CRUD 骨架，controller 仅负责参数接收与结果返回，核心逻辑已收口到 service。
- 已完成管理侧核心规则收敛：分组存在子分组时拒绝删除；叶子分组删除时级联删除其下表单；表单删除时级联删除版本与提交记录；`formKey` 仅创建时设置、创建后不可修改。
- 已完成管理页目录树后端结构收敛：目录树改为统一节点数组，节点通过 `type` 区分 `group/form`，树查询职责已拆到 `FormManageQueryService`。
- 已补齐 MyBatis-Plus 分页配置、逻辑删除、自动填充、目录树节点类型常量，以及表单定义分页查询的 `groupId + keyword` 检索与统一默认排序。

### 未完成
- 真实编译与测试执行尚未完成，当前环境缺少 Maven / Maven Wrapper，未能运行 `mvn test` 或 `mvn -DskipTests compile`。
- 更完整的数据库集成测试与版本/提交流程联调仍待后续继续补齐。

### 当前问题
- 当前代码已完成静态层面落地，但尚未经过本地 Maven 编译验证，需在具备 Maven 的环境补跑一次。

## 实施计划
- 在具备 Maven 的环境执行 `mvn test` 或至少 `mvn -DskipTests compile`，修正编译或测试暴露的问题。
- 继续补齐与表单版本保存/发布流程的联调接口，验证当前 CRUD 与后续模块的衔接。

## 执行记录
- 2026-04-09：完成表单分组/表单定义的 controller、dto/vo、service 基础 CRUD、唯一性校验、分组防环校验与级联删除链路，并补齐 MyBatis-Plus 分页配置、逻辑删除与测试骨架；当前仍缺 Maven 环境做真实编译验证。
- 2026-04-10：完成 service 对象组装与自动填充收敛，创建/更新统一改为 `BeanUtil.copyProperties`，并通过 `MetaObjectHandler` 固定自动填充 `createdBy/updatedBy = 1`。
- 2026-04-13：完成目录树结构从 `groups/rootForms/forms` 向统一节点数组的收敛，补齐节点类型常量、分组存在性校验接口简化以及目录树节点挂表单能力。
- 2026-04-14：`formKey` 创建后不可修改、表单定义分页查询改为 `groupId + keyword` 与分页默认排序下沉。
