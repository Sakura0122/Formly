# 后端表单管理

## 当前状态

### 已完成
- 已明确本阶段范围为表单分组与表单定义两个模块的后端 CRUD，controller 仅负责参数接收与结果返回。
- 已确认删除规则：分组存在子分组时后端拒绝删除；叶子分组删除时级联删除该分组下表单；表单删除时级联删除版本与提交记录。
- 已确认分组目录接口返回 `groups + rootForms` 结构，表单定义列表接口按分页查询实现。
- 已补齐 MyBatis-Plus 分页配置、mapper 扫描与逻辑删除标记，完成表单分组/表单定义的 controller、dto/vo、service CRUD 与级联删除逻辑。
- 已完成表单目录树、根级表单查询与表单定义分页查询实现，并补充参数校验与核心 service 逻辑的单测骨架。

### 未完成
- 真实编译与测试执行尚未完成，当前环境缺少 Maven / Maven Wrapper，未能运行 `mvn test` 或 `mvn -DskipTests compile`。
- 更完整的数据库集成测试与版本/提交流程联调仍待后续继续补齐。

### 当前问题
- 当前代码已完成静态层面落地，但尚未经过本地 Maven 编译验证，需在具备 Maven 的环境补跑一次。

## 实施计划
- 在具备 Maven 的环境执行 `mvn test` 或至少 `mvn -DskipTests compile`，修正编译或测试暴露的问题。
- 继续补齐与表单版本保存/发布流程的联调接口，验证当前 CRUD 与后续模块的衔接。

## 执行记录
- 2026-04-09：新建后端表单管理计划，确认本阶段交付目标为表单分组与表单定义 CRUD、树查询、分页查询、校验与级联删除。
- 2026-04-09：完成 `FormGroupController`、`FormDefinitionController`、对应 dto/vo、service 接口与实现，补齐分组树、根级表单、分页查询、唯一性校验、分组防环校验与级联逻辑删除。
- 2026-04-09：补充 MyBatis-Plus 分页配置修正、实体逻辑删除标记，以及 controller 参数校验测试、service 关键逻辑测试骨架；因本地缺少 Maven，暂未执行真实编译与测试。
- 2026-04-09：按最新约束调整依赖注入与模块调用方式，controller/service 统一改为 `@RequiredArgsConstructor`，并将跨模块删除与校验从直接访问 mapper 收口为 service 调用。
- 2026-04-09：修正分组服务与表单服务的循环依赖，保留 service 间调用约束，改为通过延迟 provider 获取跨模块 service。
