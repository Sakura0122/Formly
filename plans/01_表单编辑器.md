# 表单编辑器

## 当前状态

### 已完成
- 已完成编辑器主流程：三栏骨架、单主表格编辑、字段配置、选区、多选投放、批量删行删列、删除组件、快捷键、复制粘贴与撤销恢复已闭环。
- 已将核心状态与命令收口到 `Pinia editor store`，并完成 `EditorHeader / EditorCanvas / EditorConfigPanel / EditorHistoryPanel` 的职责下沉；`table`、历史栈与剪贴板已收敛为纯数据链路。
- 已完成草稿/发布链路：初始化、保存草稿、保存并发布、头部状态、版本表达、已保存草稿判断与最小 schema 载荷均已统一，当前仅按裸 `table` 数据传输。
- 已完成预览链路：编辑器草稿预览与管理页发布预览复用同一套渲染层，支持 `readonly / interactive`，预览窗口已改为复用命名窗口并避免误报拦截。
- 已完成历史版本模式：支持列表浏览、只读预览、显式还原、未保存隔离与时间字段收口，右侧历史预览直接复用画布只读模式。
- 已收口画布容器与右键边界：自定义右键只作用于真实表格区域，空白区恢复浏览器原生菜单。

### 未完成
- 导入、导出能力仍未接入。
- 数据源配置、字段映射、联动逻辑与表格级配置面板仍未开始。
- 多表并存、更稳定的持久化模型与更细粒度的会话恢复策略仍待明确。

### 当前问题
- 重点缺口仍在更完整的批量结构操作、表格级配置和数据配置能力。
- 行高仍以估算值为主，多组件堆叠时仍可能出现宽度挤压与高度不稳。
- 当前已接入最小持久化闭环，但撤销/重做与“已保存快照一致”的细粒度脏状态识别仍未继续收敛。

## 实施计划
- 补齐导入、导出与更完整的 schema 能力，继续完善保存后的状态表达。
- 继续增强表格结构能力，优先处理批量结构操作、表格级快捷编辑与配置面板扩展。
- 继续优化画布体验，重点处理高度估算、多组件堆叠与视觉反馈一致性。
- 继续收口会话边界，减少重复选区逻辑，并评估多表场景下的状态清理策略。

## 执行记录
- 2026-03-22 ~ 2026-04-03：完成编辑器骨架、基础字段接入、单主表格核心编辑能力，以及复制粘贴、撤销恢复、快捷键等核心交互。
- 2026-04-08：完成 `Pinia editor store` 收口，修复 `DataCloneError`，并将字段更新、历史栈与剪贴板统一到纯数据链路。
- 2026-04-14：完成 `EditorCanvas / EditorConfigPanel / EditorHeader` 对 store 的依赖下沉；已通过 `npm run type-check` 与 `npm run build`。
- 2026-04-15：完成初始化、保存草稿、保存并发布、草稿/发布状态表达、预览渲染复用、草稿预览入口与 schema 裸 `table` 契约收口；前端已通过 `npm run type-check` 与 `npm run build`，后端编译因缺少 `mvn` 暂未验证。
- 2026-04-16：完成多选投放、批量删行删列、删除组件、历史版本模式、历史预览复用、时间字段收口、画布右键边界收口与草稿预览窗口误拦截修正；已通过 `pnpm -C frontend type-check`、`pnpm -C frontend build-only` 与 `git diff --check`。
